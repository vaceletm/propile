<%= title "Version du Programme: #{@program.version}" %>

<%= content_for :sub_menu do %> 
  <%= link_to 'Edit', controller: 'programs', action: 'edit', id: @program.to_param %>
<% end %>
<%= content_for :extra_info do %> 
  <i> Cette page est pour les modérateurs seulement </i>
<% end %>

<%= render 'programs/program_matrix', :program => @program, :editable => false %>

<div class="alignright">
  Créé le: <%= l @program.created_at %> -- 
  Modifié le: <%= l @program.updated_at %> 
</div>

