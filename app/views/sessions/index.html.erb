<%= content_for :sub_menu do %> 
  <%= link_to 'Export CSV', csv_sessions_path %>
  <%= link_to 'Toute l'activité en RSS', :controller => :sessions , :action => 'activity_rss', :format => :xml %>
  <%= link_to 'Personas', personas_sessions_path %>
<% end %>

<%= render 'sessions/index', :sessions => @active_sessions, :sortable => true  %>
<h3>ANNULEES</h3>
<%= render 'sessions/index', :sessions => @canceled_sessions, :sortable => true  %>
