<%= title "Revue pour: #{@review.session.title}" %> 
<%= content_for :sub_menu do %> 
  <%= guarded_link_to 'Editer', controller: 'reviews', action: 'edit', id: @review  %> 
  <%= link_to 'Retour à Session', @review.session %>
<% end %>
<%= render 'sessions/show_short', :session => @review.session  %>
<hr />

<%= render 'reviews/show', :review => @review  %>

