
<h3>Revue par <%= link_to w(review.presenter.name), review.presenter %> </h3>

Ce que j'aime:
<div class="lessoutlined"> 
  <%= wikinize(review.things_i_like) %>
</div>
Les pistes d'améliorations':
<div class="lessoutlined"> 
  <%= wikinize(review.things_to_improve) %>
</div>
<div class="reviewcontenttitle">
  La session a optenu <%= review.score %> / 10.
</div>

<% if !review.new_record? %> 
<div class="alignright">
  Revue crée le: <%= l review.created_at %> -- 
  Modifié le: <%= l review.updated_at %> 
  par <%= link_to w(review.presenter.name), review.presenter %>
</div>
<% end %>
