<%= title "Nouveau Commentaire sur la revue pour: #{@new_comment.review.session.title}" %>

<%= content_for :sub_menu do %> 
  <%= link_to 'Retour à la session', @new_comment.review.session %>
<% end %>

<%= content_for :extra_info do %> 
  <div><i>Le but de cette revue est d'aider à améliorer cette session proposée.</i></div>
<% end %>

<%= render 'form', :comment=>@new_comment %>

