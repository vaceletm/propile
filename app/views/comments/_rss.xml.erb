<item>
  <title><![CDATA[<%= w this_session.title%> - Revue par <%= w review.presenter.name %> - Commentée par <%=  w comment.presenter.name %>]]></title>
  <link><%= url_for :action => 'show' , :controller => 'comments' , :id => comment, :only_path => false %></link>
  <description><![CDATA[
    <p>Commenté par  <a href="<%= url_for :action => 'show' , :controller => 'presenters' , :id => comment.presenter , :only_path => false %>"><%=  w comment.presenter.name %></a>
      sur <a href="<%= url_for :action => 'show' , :controller => 'reviews' , :id => review , :only_path => false %>">revue</a>
      de <a href="<%= url_for :action => 'show' , :controller => 'sessions' , :id => this_session , :only_path => false %>"><%= w this_session.title%></a>
      par <a href="<%= url_for :action => 'show' , :controller => 'presenters' , :id => review.presenter , :only_path => false %>"><%= w review.presenter.name %></a></p>
    <%= wikinize( comment.body ) %>
    ]]></description>
  <pubDate><%= comment.updated_at.strftime("%a, %-d %b %Y %H:%I:%M GMT") %></pubDate>
  <guid><%= url_for :action => 'show' , :controller => 'comments' , :id => comment, :only_path => false %></guid>
</item>
